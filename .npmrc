# Support pnpm install on host for both macOS and Linux binaries
# This ensures Docker containers (mounting node_modules) find the correct native binaries
supported-architectures=os:linux,os:darwin,cpu:arm64,cpu:x64
