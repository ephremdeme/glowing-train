{
  "name": "@cryptopay/ops-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "ops-cli": "./src/cli.ts"
  },
  "scripts": {
    "dev": "tsx src/cli.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@cryptopay/auth": "workspace:*"
  }
}
